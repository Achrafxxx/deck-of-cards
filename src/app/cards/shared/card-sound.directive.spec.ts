import { CardSoundDirective } from './card-sound.directive';

describe('CardSoundDirective', () => {
  it('should create an instance', () => {
    const directive = new CardSoundDirective();
    expect(directive).toBeTruthy();
  });
});
